{
  "name": "certmatrix",
  "version": "1.0.0",
  "main": "index.js",
   "scripts": {
    "dev": "tinacms dev -c \"next dev\"",
    "build": "tinacms build && next build",
    "start": "tinacms build && next start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "tinacms": "^2.8.1"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.10.1",
    "@types/node": "^24.1.0"
  }
}
